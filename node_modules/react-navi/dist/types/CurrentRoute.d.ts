import * as React from 'react';
import { Route } from 'navi';
export declare function useCurrentRoute(): Route<any>;
export interface CurrentRouteProps {
    /**
     * A render function that can be used to access the current route.
     */
    children: (route: Route) => React.ReactNode;
}
export declare namespace CurrentRoute {
    type Props = CurrentRouteProps;
}
export declare function CurrentRoute(props: CurrentRouteProps): JSX.Element;
