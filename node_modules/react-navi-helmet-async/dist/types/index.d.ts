import { Chunk } from 'navi';
import * as React from 'react';
import { Helmet } from 'react-helmet-async';
interface NaviHelmetProviderProps {
    canUseDOM?: boolean;
    children: React.ReactNode;
    context?: any;
}
declare function NaviHelmetProvider({ canUseDOM, context, children, }: NaviHelmetProviderProps): React.ReactElement<any>;
export declare function renderViewHead(chunks: Chunk[]): React.ReactNode;
export { NaviHelmetProvider as HelmetProvider };
export { Helmet };
export default NaviHelmetProvider;
